/**
 * Testing entry point for the VDL Plugin SDK.
 *
 * Import `irb` from here when you need compact factory helpers for building
 * realistic plugin input and IR fixtures in tests.
 *
 * @example
 * ```ts
 * import { irb } from "@varavel/vdl-plugin-sdk/testing";
 *
 * const input = irb.pluginInput();
 * ```
 */
export { irb } from "./ir-builders";
